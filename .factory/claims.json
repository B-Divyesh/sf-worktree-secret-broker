[
  {
    "id": "demo-isolated",
    "claim": "The CLI demo reads no provider, makes no network connection, and removes its temporary worktree.",
    "where": "README and demo output",
    "test": "npm test -- --grep @claim:demo-isolated",
    "sandbox": "fresh temp directory with provider and AF_INET socket traps; bundled CLI demo"
  },
  {
    "id": "approved-environment",
    "claim": "Only approved values enter a cleared child environment.",
    "where": "landing hero, how it works, privacy page, README",
    "test": "npm test -- --grep @claim:approved-environment",
    "sandbox": "temp Git worktree, fake Secret Service CLI, unrelated parent token"
  },
  {
    "id": "names-only-receipt",
    "claim": "Receipts, worktrees, and provider-reference config contain names or references, never resolved values.",
    "where": "landing facts and preview; README",
    "test": "npm test -- --grep @claim:names-only-receipt",
    "sandbox": "temp Git worktree and fake provider; inspect stdout, config, and git diff"
  },
  {
    "id": "production-denied",
    "claim": "Production-labelled mappings are denied by default.",
    "where": "landing limits and README",
    "test": "npm test -- --grep @claim:production-denied",
    "sandbox": "fresh production-labelled sample config with no provider installed"
  },
  {
    "id": "lease-expiry",
    "claim": "Expiry stops the child process and records lease-expired.",
    "where": "landing how it works and README",
    "test": "npm test -- --grep @claim:lease-expiry",
    "sandbox": "temp Git worktree, fake provider, one-second test lease, sleeping child"
  },
  {
    "id": "worktree-root-required",
    "claim": "The broker refuses to start a child outside a named Git worktree root.",
    "where": "landing how it works and README",
    "test": "npm test -- --grep @claim:worktree-root-required",
    "sandbox": "temp Git worktree with a nested directory and fake provider"
  },
  {
    "id": "demo-same-origin",
    "claim": "The browser demo uses bundled sample text and contacts no third party.",
    "where": "demo, privacy page, and README",
    "test": "npm test -- --grep @claim:demo-same-origin",
    "sandbox": "fresh browser context with every request origin recorded"
  },
  {
    "id": "recorded-demo-sample",
    "claim": "The landing recording uses the CLI demo’s two bundled names.",
    "where": "landing preview",
    "test": "npm test -- --grep @claim:recorded-demo-sample",
    "sandbox": "fresh browser context plus bundled CLI demo JSON receipt"
  },
  {
    "id": "site-no-analytics",
    "claim": "The site has no analytics and makes no third-party request on the landing page.",
    "where": "privacy page",
    "test": "npm test -- --grep @claim:site-no-analytics",
    "sandbox": "fresh browser context at the landing page with every request origin recorded"
  },
  {
    "id": "policy-generator",
    "claim": "The local policy helper turns variable names into development-only provider references without a network request.",
    "where": "landing policy helper and README",
    "test": "npm test -- --grep @claim:policy-generator",
    "sandbox": "fresh browser context with request origins recorded and a sentinel secret value"
  },
  {
    "id": "broker-stop-revokes",
    "claim": "Stopping the broker kills the leased child process and records broker-stopped.",
    "where": "README exposure limits",
    "test": "npm test -- --grep @claim:broker-stop-revokes",
    "sandbox": "temp Git worktree, fake provider, long-running child group with a background descendant; probe SIGINT, SIGTERM, SIGHUP, one-second expiry, and forced broker-parent death; assert a names-only receipt and no surviving group member"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo clears demo-only session data and restores the first recorded output.",
    "where": "demo banner, privacy page, and demo documentation",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh browser context with changed demo-prefixed session data"
  },
  {
    "id": "demo-browser-isolation",
    "claim": "The browser demo changes only demo-prefixed storage and Start for real removes that demo storage.",
    "where": "demo banner and demo documentation",
    "test": "npm test -- --grep @claim:demo-browser-isolation",
    "sandbox": "fresh browser context with real-data sentinels, entered through ?demo=1, then reset and exited"
  },
  {
    "id": "recorded-demo-receipt",
    "claim": "The browser recording shows the bundled CLI demo receipt names, timing, outcome, and revocation fields.",
    "where": "demo recording and landing step 3",
    "test": "npm test -- --grep @claim:recorded-demo-receipt",
    "sandbox": "fresh browser context at ?demo=1 plus wsb demo --json; compare every shown receipt field and verify CLI cleanup"
  },
  {
    "id": "copy-install-command",
    "claim": "Copy install command copies the public-source Cargo command.",
    "where": "landing install action",
    "test": "npm test -- --grep @claim:copy-install-command",
    "sandbox": "fresh browser context with clipboard permission"
  },
  {
    "id": "one-password-provider",
    "claim": "The broker reads a documented op://VAULT/ITEM/FIELD reference through 1Password CLI.",
    "where": "README provider documentation",
    "test": "npm test -- --grep @claim:one-password-provider",
    "sandbox": "temp Git worktree and fake op executable that verifies the read arguments"
  },
  {
    "id": "os-keychain-provider",
    "claim": "The broker reads keychain://SERVICE/ACCOUNT through Secret Service on Linux or Keychain on macOS and passes only the approved value.",
    "where": "landing how it works and README provider documentation",
    "test": "npm test -- --grep @claim:os-keychain-provider",
    "sandbox": "temp Git worktree and fake secret-tool executable; shared command-contract unit test asserts Linux secret-tool and macOS security arguments"
  },
  {
    "id": "policy-helper-input-boundary",
    "claim": "The local policy helper rejects duplicate names and has no control for secret values.",
    "where": "README team policy helper",
    "test": "npm test -- --grep @claim:policy-helper-input-boundary",
    "sandbox": "fresh browser context; inspect every form control, submit duplicate names, assert actionable error, no TOML, and same-origin requests"
  }
]
