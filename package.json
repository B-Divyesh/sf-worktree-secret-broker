{
  "name": "worktree-secret-broker-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts",
    "build:site": "vite build --config vite.config.ts && node scripts/prepare-site.mjs",
    "build:cli": "cargo build --release && node scripts/copy-binary.mjs",
    "build": "npm run build:site && npm run build:cli",
    "typecheck": "tsc --noEmit",
    "check:macos": "cargo check --target x86_64-apple-darwin --locked",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@types/node": "24.3.0",
    "@playwright/test": "1.58.2",
    "typescript": "5.9.2",
    "vite": "7.3.6"
  }
}
