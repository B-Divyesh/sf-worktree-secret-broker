# Contributing

Install Rust, Node.js 20 or newer, and npm. Run `npm install`, `npm test`, and
`npm run build` before opening a change. Never add real vault references,
tokens, `.env` files, or generated secret values to fixtures.
